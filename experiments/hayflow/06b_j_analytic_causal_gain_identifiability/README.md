# 06b-j — analytic causal-gain identifiability

This component playground follows the negative 06b-i voltage-objective result.
It removes neural optimization from the causal-gain hypothesis and fits bounded
closed-form lookup tables on the same historically reused train roles.

The synchronized causal hierarchy is global, region, raw predicted activity,
current-voltage band, region-by-raw-activity and
region-by-raw-activity-by-current-voltage. The primary comparison is the final
causal partition against the global lookup. A teacher-activity lookup is an
oracle diagnostic only: it is reported separately and cannot be selected.

No validation/test state is accessed, no new independent confirmation is
claimed, and this experiment cannot authorize 06c or full training. A causal
success can authorize only confirmation on fresh train support.
